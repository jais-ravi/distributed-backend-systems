# MongoDB Atlas Connection String
# Replace <username>, <password>, and <cluster-url> with your Atlas details.
# Ensure you include the database name (e.g., /distributed_backend) at the end.
DATABASE_URL=mongodb+srv://<username>:<password>@<cluster-url>/distributed_backend?retryWrites=true&w=majority

# Upstash Redis Connection String
REDIS_URL=redis://default:<password>@<cluster-url>:6379
